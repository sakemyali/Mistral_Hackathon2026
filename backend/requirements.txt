fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=12.0
mss>=10.0.0
pytesseract>=0.3.10
Pillow>=10.0.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.0.0
mistralai>=1.6.0
wandb>=0.15.0

# Note:
# - Frontend error "vite: command not found" is fixed by running `npm install` in ../frontend.
# - Backend error "No module named 'elevenlabs'" is fixed by installing the package below.
elevenlabs>=1.0.0
